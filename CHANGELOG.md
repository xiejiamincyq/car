# Changelog

本项目遵循 [Keep a Changelog](https://keepachangelog.com/zh-CN/1.1.0/) 的记录方式。

## [Unreleased]

### Added

- 新增超载核心玩法：比赛中双击 `W` 可在 2.5 秒内渐进获得最多 100 km/h 的临时速度上限和额外加速度，同时逐步消耗最大油箱的 10%；支持燃油门槛、不可叠加、冷却、暂停冻结与重开复位。
- 新增原创 64 秒复古电子循环曲 `Neon Coast Circuit`，包含可重复的 NumPy/FFmpeg 生成源、构建报告、OGG 循环导入设置与曲目目录安全回退；游戏内预留 4 dB 关键音效余量。
- 新增无声安全的 `MusicDirector` 生命周期接口，覆盖赛道选择、倒计时淡入、比赛、暂停、恢复倒计时、结算淡出和资源释放；真实曲目缺失时不会影响现有玩法。
- 新增 `AudioDirector` 统一管理音效播放器、提示音状态、Music/Effects 总线路由和全局静音。

### Changed

- `main.gd` 不再直接创建和编排成组音频播放器，由 1288 行收敛到约 1150 行；Music 与 Effects 音量现可独立应用并写回现有 v5 存档字段。
- NPC 车辆在生成时获得可复现且全程固定的世界速度；普通交通不再被安全钳位暗中改速，画面位移统一由玩家速度与 NPC 速度之差计算，使减速绕后、再加速超车成为可用策略。卡车继续保持普通交通速度的 80%，高速超车车也不再使用玩家速度派生的动画速度。
- 普通交通改为同帧统一决策、再统一推进，防止前后车辆读取不同时间点的位置；生成、随机变道、施工预警和制动反应距离负责提前回避无解交通组合。

### Fixed

- NPC 变道提示现在只在目标车道安全、玩家仍有逃生路线且车辆能在可见区域开始横移时出现；正常情况下提示会锁定变道承诺，不再因宽泛反应区的重复判定而亮箭头却放弃动作，迫近玩家最后出口时仍保留紧急取消保护。
- NPC 横向变道期间会持续向目标方向倾斜 15°，与玩家车辆的转向反馈一致，并在到达目标车道后回正。
- NPC 随机变道现会更早进入可见预警，并缩短为 0.66–0.59 秒，保证最慢车辆面对最高速玩家时仍在被超过前开始横移；同步保留安全反应下限与转向提示音。
- NPC 变道箭头改到车体左右外侧并明确指向目标车道，增加光晕和描边；移除所有覆盖在 NPC 车身中央的圆形与字符标志。
- 修复固定赛道试玩启动器重置音频后未进入音乐倒计时生命周期，导致霓虹海岸技术样曲已接入却完全不播放的问题。

## [0.3.0] - 2026-08-10

### Added

- 新增与夜间海岸风格一致的原创玩家车、轿车、厢式车、掀背车、跑车、长货车和燃油拾取像素精灵；保留车型符号与高对比反馈，不改变碰撞边界和玩法参数。
- 新增可无缝滚动的原创道路、岩岸植被与海面浪花，环境周期与车道线周期解耦。
- 新增 HUD 外框、燃油/赛程仪表、事件铭牌、实体封路路障与通关徽章。
- 新增加速喷焰、制动尾灯与拖痕、拾取扩散环、超车/近失速度线及通关爆发动画。

### Changed

- 车辆改用近俯视投影与等比缩放，不同交通行为拥有更清楚的车身轮廓。
- 瞬时拾取与超车特效具有固定生命周期和对象上限，减少闪烁设置继续生效。

## [0.2.0] - 2026-08-09

### Added

- 新增 UI 移动/确认/取消、近失、连击、倒计时、检查点、通关、低燃油和封路预警程序化音效；统一路由至 `Master`/`Effects` 总线，并预留 `Music` 总线。
- 新增完整标题界面、难度入口、声音设置、操作说明和键盘焦点导航。
- 新增三秒开局/恢复倒计时、正式 HUD、暂停菜单、破坏性操作二次确认，以及失败/通关两类结算界面。
- 结算现可展示得分、距离、超车、近失占位、到达赛段与局种子，并支持不重启程序连续重玩。
- 新增可可靠识别 Godot 脚本断言失败的统一 PowerShell 测试入口。
- 新增带版本迁移、严格类型校验和替换失败恢复的本地存档模型。
- 标题与结算新增前五名成绩、`NEW RECORD` 和生涯统计持久化；声音、静音与难度选择可跨进程保留。
- 超车与近失改为按车辆实际通过过程一次性结算，碰撞车和后方高速车不会误领奖励。
- 新增保守连击倍率：有效通行可延续连击，碰撞或超时清零，HUD 显示当前倍率与剩余窗口。
- 碰撞新增有界程序化火花与速度缩放震动；低燃油采用两档文字警告，赛段变化提供文字横幅和道路配色渐变。
- 新增四赛段有限赛程：三处检查点提供明确提示和少量燃油，抵达 3200 米终点后进入正式通关结算。
- 最终赛段新增低速长车身货车；绘制、碰撞、超车结算、生成间距和相邻车道错峰均使用实际车身长度。
- 中后期新增种子驱动的单车道封路事件：先清场和预警，再短时封闭；事件期间禁止冲突出生并持续保留玩家逃生走廊。
- 轻松、标准、困难三档现会通过集中配置调整燃油压力、交通密度、封路频率和连击宽容度，三档均共享相同公平性规则。
- 新增 `F11` 与设置菜单全屏切换并跨进程保存；720p、1080p 的 HUD 和菜单布局纳入自动回归。
- 新增中文、英文与跟随系统三档语言设置；标题、菜单、HUD、反馈和结算文案统一集中管理并即时切换。
- 新增高对比/色弱友好配色、减少闪烁和屏幕震动开关；车辆使用独立车顶标记，封路和燃油提示保留非颜色语义。

### Changed

- 将 0.1.0-rc.1 的定位明确为核心循环纵向切片，并建立通往 0.2.0 完整玩法闭环的分阶段计划。
- 同步项目名称、历史任务完成状态和人工试玩记录字段。
- 正常重开会生成新的可记录局种子，同时允许使用指定种子复现交通序列。
- 燃油补给改为随局种子变化的安全车道选择；当前及相邻车道均不可达时延迟生成。
- 稳态车与预告变道车采用一致纵向速度并错峰出生，避免相邻车道形成移动墙；后方高速车会在逃生路线受阻时延后出击。
- 运行或倒计时中窗口失焦会立即暂停并停止游戏音频；回焦后必须主动确认并经过安全倒计时。
- 存档格式升级至 v4，旧 v1–v3 设置和生涯数据会保留，并补入安全的窗口、语言与可访问性默认值。

### Fixed

- 修复 README 和界面已承诺但未实际注册的 `R` 重开键；比赛中使用现有确认流程，结算页可直接重开。
- 倒计时归零时新增独立的 `GO` 提示音；封路预警改为更醒目的三连警报，并在正式封闭时播放确认音。
- 封闭车道现会形成实体施工隔离带：玩家无法驶入，强闯会被推回开放车道、降速并打断连击，视觉改为横向路障和 X 标记。
- 手动暂停/继续键由 `P / Esc` 改为空格键，避免与菜单返回及系统切窗操作混淆；`Alt+Tab` 仍触发失焦安全暂停。
- 交通等待线与回收线改为跟随视口高度，避免 1080p 下车辆在经过玩家前被错误回收。
- 调整燃油供需曲线，避免低速收集全部补给后形成无限燃油正收益。
- 延长后期变道预警，使其始终覆盖完整换道时间和最低反应余量。

## [0.1.0-rc.1] - 2026-07-29

### Added

- 可用键盘完成的完整单局循环：开始、驾驶、交通风险、燃油、计分、暂停、结算和重开。
- 原创霓虹海岸视觉、程序化音效及音量控制。
- Windows Desktop 导出预设，以及覆盖真实燃油结算、第二局重开、难度/车种和多种玩家车道/速度组合的发布回归。
- 音频场景测试的显式停止、流释放和清理帧 teardown。
- 面向试玩者的运行、测试、导出、发布和回滚说明。

### Security

- 未加入联网、账号、遥测或外部数据收集功能。
