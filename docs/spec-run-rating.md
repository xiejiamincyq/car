# P5 完赛评分与 v6 存档

2026-09-08：B2 碰撞仿真与难度损伤倍率人工试玩通过。此规格细化既有评分计划，不改变碰撞、奖牌或解锁规则。

## 计算规则

仅完赛且比赛用时有效时评级，失败局不授予等级。用时采用比赛累计时间，不含倒计时与暂停。

- 时间分：`round(40 × clamp(2 − 用时 / 基准时间, 0, 1))`。基准内满分，两倍基准及以上为零。
- 超车分：`round(20 × clamp(超车数 / 目标超车数, 0, 1))`。
- 避撞分：`max(0, 20 − 4 × 有效碰撞次数)`。沿用碰撞系统的接触去重，不按每帧重复计数；锥桶有效碰撞也计入。
- 金币分：`round(20 × clamp(金币数 / 目标金币数, 0, 1))`。
- 各分项分别四舍五入后相加，满分100。S≥90、A≥80、B≥70、C≥60，其余D。
- 未使用完整度损伤倍率折算碰撞次数；简单0.2、标准0.5、困难1.0只作用于完整度损伤。

## 初始平衡参数（P7 人工验证后冻结）

| 赛道 | 满分基准秒数 | 目标超车数 | 目标金币数 |
|---|---:|---:|---:|
| 霓虹海岸 | 60 | 12 | 60 |
| 货运港湾 | 66 | 14 | 64 |
| 风暴山脊 | 66 | 14 | 62 |
| 日出快线 | 65 | 16 | 68 |

四赛道长度3200–3600米，基础极速约76米/秒，无遮挡理论约42–47秒；时间目标预留了加速和绕行余量。这只是起始估计，不代表已经完成四赛道、六车型、三难度的可达性验证。不同难度暂用相同评分目标；评级不额外增加奖励、改变奖牌或车辆解锁。

## 存档与覆盖

- v6 新增独立 `ratings.track_bests` 字段，逐赛道保存 `{total, grade}`；首次完赛即使0分也保存D。
- 更高总分才覆盖；同分保留原记录。失败、无效数据不产生评分。
- 本局四项拆分保留在运行时，和历史最佳评分分开；不组合不同局的最高分项。
- v1–v4沿用已有迁移补默认值；v5完整保留设置、榜单、生涯、巡回成绩和解锁，仅补空评分记录。v0兼容逻辑保持。
- 读取迁移只在内存进行，不回写文件。保存仍使用临时文件与备份恢复机制；测试仅用独立测试档，不操作个人存档。
- 较旧程序不识别v6。回滚程序前应保留升级前的个人存档副本，不用旧版覆盖v6档案。

## 验证边界

纯评分边界、失效输入、分项上限、最佳成绩覆盖、四赛道隔离、真实ConfigFile v5迁移、无效写入保护，以及主场景金币/碰撞接线、重复结算和重开清理均有回归测试。P6完成后再做可见评分卡的人工验收，P7再验证目标值是否合理。

## P6 B布局与新增油耗倍率

用户批准B四项成绩卡。日常油耗=基础油耗×难度倍率×（阻力负荷+正加速度负荷）；简单0.8、标准1.2、困难2.0。重复切换/配置难度不叠乘。超载固定15%成本保持原规格，完整度损伤倍率保持0.2/0.5/1.0。

批准新验收前的燃油预算审计覆盖恒定极速/从280加速两个工况×三难度×四赛道×六车型，共144组合；不拾取沿途燃油，但包含检查点自动补给。7个困难组合未完赛，另外6个组合剩余油量不足5。例：困难driftwing加速后在日出快线3306.8/3600米耗尽燃油。简单/标准未发现此预算违例。该轮保留旧验收时，全量回归报告了燃油预算失败，未将其误报为通过。

后续人工批准：困难模式允许依赖沿途补油。预算验收改为困难在赛程中点获得一次标准燃油拾取（24），仍要求完赛且剩余≥5；简单/标准保持零沿途拾取条件。该测试验证燃油预算，不代表真实车流下必能拿到某个油箱；生成安全、可达性和人工驾驶仍分别验证。未降低×2.0，也未增加实际游戏补给。

P7新增216组交通模拟：四赛道×六车型×三难度×种子611/2026/9001，每组60秒，含加速、减速与超载速度窗口。检查活动交通安全约束与对象数量上限，不模拟真实玩家碰撞、金币收集决策或评分可达性；不能替代困难补油路线和评分手感的人工试玩。
